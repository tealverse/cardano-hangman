# cardano-hangman

```
git clone https://github.com/input-output-hk/plutus-starter
cd plutus-starter
nix-shell
in der nix shell:
cabal --enable-nix update
cabal --enable-nix build plutus-example
codium .
```


```
cabal configure -f defer-plugin-errors

cabal configure
```